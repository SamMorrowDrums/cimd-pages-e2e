# STEPS.md — evidence log

This log records the actual commands run and their real observed output
for the current (simplified, self-hosted) version of this repo. It will be
filled in immediately after pushing, once GitHub Pages has republished
`client.json` and `test-fixtures/mismatch-client.json`.
